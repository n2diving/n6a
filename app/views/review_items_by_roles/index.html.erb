<p id="notice"><%= notice %></p>

<h1>Review Items By Roles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @review_items_by_roles.each do |review_items_by_role| %>
      <tr>
        <td><%= link_to 'Show', review_items_by_role %></td>
        <td><%= link_to 'Edit', edit_review_items_by_role_path(review_items_by_role) %></td>
        <td><%= link_to 'Destroy', review_items_by_role, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Review Items By Role', new_review_items_by_role_path %>
